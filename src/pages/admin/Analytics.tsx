import { useQuery } from '@tanstack/react-query';
import { Area, AreaChart, Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { api } from '../../lib/api';
import { cardStyle } from '../../lib/cardStyle';

interface AnalyticsResponse { enrollmentTrend: { month: string; count: number }[]; usageTrend: { month: string; count: number }[]; roleCounts: Record<string, number>; totals: { children: number; users: number; totalXp: number; badgeUnlockCount: number; practiceSessions: number } }
const COLORS = ['--color-brand-lavender', '--color-brand-coral', '--color-brand-teal', '--color-brand-sun', '--color-brand-violet'];

function EmptyChart({ label }: { label: string }) { return <div className="flex h-full items-center justify-center rounded-2xl border border-dashed border-[var(--color-border)] bg-white/35 px-4 text-center text-sm text-[var(--color-text-muted)]">Wala pang datos sa {label}.</div>; }
const chartMargins = { left: -20, right: 10, top: 8, bottom: 0 };

export default function AdminAnalytics() {
  const { data, isLoading } = useQuery({ queryKey: ['admin-analytics'], queryFn: () => api<AnalyticsResponse>('/admin/analytics', { auth: true }) });
  const roleData = data ? Object.entries(data.roleCounts).sort(([, a], [, b]) => b - a).map(([role, count]) => ({ role, count })) : [];
  const cards = data ? [
    { icon: '👥', label: 'Mga gumagamit', value: data.totals.users, note: 'Lahat ng account' }, { icon: '🧒', label: 'Mag-aaral', value: data.totals.children, note: 'Mga nakatalang bata' },
    { icon: '🎙', label: 'Pagsasanay', value: data.totals.practiceSessions, note: 'Mga sesyon ng pagsasanay' }, { icon: '★', label: 'Kabuuang XP', value: data.totals.totalXp, note: 'Puntos sa pagkatuto' },
    { icon: '🏅', label: 'Mga Parangal', value: data.totals.badgeUnlockCount, note: 'Mga nakuhang parangal' },
  ] : [];
  return <div className="flex min-w-0 flex-col gap-6">
    <header><p className="text-xs font-extrabold tracking-[.12em] text-[var(--color-primary)] uppercase">Pananaw sa sistema</p><h1 className="text-2xl font-extrabold sm:text-3xl">Pagsusuri</h1><p className="mt-1 max-w-2xl text-sm text-[var(--color-text-muted)]">Pagpapalista, paggamit, pakikibahagi sa pagkatuto, at distribusyon ng gumagamit mula sa kasalukuyang datos ng sistema.</p></header>
    {isLoading ? <div className="grid grid-cols-2 gap-3 lg:grid-cols-5">{Array.from({ length: 5 }, (_, index) => <div key={index} className="h-28 animate-pulse rounded-3xl border bg-white/45" />)}</div> : data && <>
      <section className="grid grid-cols-2 gap-3 lg:grid-cols-5">{cards.map((card, index) => <article key={card.label} className="rounded-3xl border p-4 shadow-card" style={cardStyle(COLORS[index], 7, 25)}><div className="flex items-center justify-between"><span className="flex h-10 w-10 items-center justify-center rounded-2xl bg-white/70 text-lg" aria-hidden="true">{card.icon}</span><span className="h-2.5 w-2.5 rounded-full" style={{ backgroundColor: `var(${COLORS[index]})` }} /></div><p className="mt-3 text-2xl font-extrabold tabular-nums sm:text-3xl">{card.value.toLocaleString()}</p><p className="text-sm font-bold">{card.label}</p><p className="mt-1 text-xs text-[var(--color-text-muted)]">{card.note}</p></article>)}</section>
      <div className="grid min-w-0 grid-cols-1 gap-5 xl:grid-cols-2">
        <section className="min-w-0 rounded-3xl border p-4 shadow-card sm:p-6" style={cardStyle('--color-brand-lavender', 5, 22)}><div><p className="text-xs font-bold tracking-wide text-[var(--color-brand-violet)] uppercase">Paglago</p><h2 className="text-xl font-extrabold">Pagpapalista bawat buwan</h2><p className="text-sm text-[var(--color-text-muted)]">Bilang ng mga bagong mag-aaral sa paglipas ng panahon.</p></div><div className="mt-4 h-72">{data.enrollmentTrend.length ? <ResponsiveContainer width="100%" height="100%"><AreaChart data={data.enrollmentTrend} margin={chartMargins}><defs><linearGradient id="adminEnrollment" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="var(--color-primary)" stopOpacity={0.42} /><stop offset="100%" stopColor="var(--color-primary)" stopOpacity={0.03} /></linearGradient></defs><CartesianGrid strokeDasharray="4 4" vertical={false} stroke="rgba(60,60,100,.12)" /><XAxis dataKey="month" tick={{ fontSize: 11 }} axisLine={false} tickLine={false} /><YAxis allowDecimals={false} tick={{ fontSize: 11 }} axisLine={false} tickLine={false} /><Tooltip contentStyle={{ borderRadius: 14, border: '1px solid var(--color-border)' }} /><Area type="monotone" dataKey="count" name="Mga nagpatala" stroke="var(--color-primary)" strokeWidth={3} fill="url(#adminEnrollment)" /></AreaChart></ResponsiveContainer> : <EmptyChart label="pagpapalista" />}</div></section>
        <section className="min-w-0 rounded-3xl border p-4 shadow-card sm:p-6" style={cardStyle('--color-brand-teal', 5, 22)}><div><p className="text-xs font-bold tracking-wide text-[var(--color-brand-teal)] uppercase">Pakikibahagi</p><h2 className="text-xl font-extrabold">Paggamit ng pagsasanay</h2><p className="text-sm text-[var(--color-text-muted)]">Bilang ng mga sesyon ng pagsasanay sa pagbigkas bawat buwan.</p></div><div className="mt-4 h-72">{data.usageTrend.length ? <ResponsiveContainer width="100%" height="100%"><AreaChart data={data.usageTrend} margin={chartMargins}><defs><linearGradient id="adminUsage" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="var(--color-brand-teal)" stopOpacity={0.42} /><stop offset="100%" stopColor="var(--color-brand-teal)" stopOpacity={0.03} /></linearGradient></defs><CartesianGrid strokeDasharray="4 4" vertical={false} stroke="rgba(60,60,100,.12)" /><XAxis dataKey="month" tick={{ fontSize: 11 }} axisLine={false} tickLine={false} /><YAxis allowDecimals={false} tick={{ fontSize: 11 }} axisLine={false} tickLine={false} /><Tooltip contentStyle={{ borderRadius: 14, border: '1px solid var(--color-border)' }} /><Area type="monotone" dataKey="count" name="Mga sesyon" stroke="var(--color-brand-teal)" strokeWidth={3} fill="url(#adminUsage)" /></AreaChart></ResponsiveContainer> : <EmptyChart label="pagsasanay" />}</div></section>
      </div>
      <section className="min-w-0 rounded-3xl border p-4 shadow-card sm:p-6" style={cardStyle('--color-brand-sun', 5, 22)}><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold tracking-wide text-[var(--color-warning-text)] uppercase">Mga gumagamit</p><h2 className="text-xl font-extrabold">Mga gumagamit bawat tungkulin</h2><p className="text-sm text-[var(--color-text-muted)]">Distribusyon ng mga account sa buong plataporma.</p></div><span className="rounded-full bg-white/70 px-3 py-1 text-xs font-bold">{data.totals.users.toLocaleString()} lahat</span></div><div className="mt-4 h-72">{roleData.length ? <ResponsiveContainer width="100%" height="100%"><BarChart data={roleData} margin={chartMargins}><CartesianGrid strokeDasharray="4 4" vertical={false} stroke="rgba(60,60,100,.12)" /><XAxis dataKey="role" tick={{ fontSize: 11 }} axisLine={false} tickLine={false} /><YAxis allowDecimals={false} tick={{ fontSize: 11 }} axisLine={false} tickLine={false} /><Tooltip contentStyle={{ borderRadius: 14, border: '1px solid var(--color-border)' }} /><Bar dataKey="count" name="Mga gumagamit" fill="var(--color-brand-violet)" radius={[10, 10, 0, 0]} maxBarSize={64} /></BarChart></ResponsiveContainer> : <EmptyChart label="tungkulin" />}</div></section>
    </>}
  </div>;
}
